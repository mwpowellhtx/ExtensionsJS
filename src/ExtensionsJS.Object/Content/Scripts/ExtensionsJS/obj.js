//// TODO: TBD: thought better of this, but will capture this for posterity nonetheless

//// TODO: TBD: there may be better ways?
//function clone(instance, init = function (x) {}) {
//    var cloned = instance.clone();
//    init(cloned);
//    return cloned;
//}
